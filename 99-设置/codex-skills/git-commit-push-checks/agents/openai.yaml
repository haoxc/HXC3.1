interface:
  display_name: "Git C&P Checks"
  short_description: "提交和推送前检查大文件、归档附件并验证 Obsidian 链接"
  default_prompt: "Use $git-commit-push-checks for a safe c&p workflow: archive large Obsidian attachments, audit >15MB files, commit, and push."
policy:
  allow_implicit_invocation: true
