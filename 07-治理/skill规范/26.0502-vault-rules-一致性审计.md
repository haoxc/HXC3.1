---
tags: [治理, vault-rules, skill规范, audit]
description: vault-rules.yaml 声明层交叉一致性审计：SVG 孤儿豁免、manifest 结构化 vault_rules 段。3 PASS / 3 FIX
type: log
create-date: 2026-05-02
---

## 审计范围

交叉验证三组文件链：`vault-rules.yaml` ↔ `agent-skills/*/manifest.yaml` ↔ `agent-skills/*/core.md`

## 检查清单

| 检查项 | 状态 | 处置 |
|--------|------|------|
| skill规范 subdir 命名（semantic rule） | ✓ | — |
| 整改台账命名 `{yy.MMdd}-{主题}.md` | ✓ | — |
| manifest 文本引用 vault-rules | ✓ | — |
| core.md frontmatter | ✓ | 模板无 frontmatter 属设计行为 |
| SVG 孤儿检测 | ⚠ 5 个 | 全部在 `---/workspaces/`，worktree 内另有 10 个 |
| manifest related_skills | ✓ | 误判，原已有声明（在 runtime frontmatter 内） |
| manifest vault_rules 结构化段 | ⚠ 缺失 | 新增 |

## 处置

### SVG 豁免规则（vault-rules.yaml）

在 `cross_reference` 下新增 `svg_scope` + `exclude` 字段，明确规则适用范围为知识目录（01~10），豁免 `---/workspaces` 和 `.claude/worktrees`。

```yaml
svg_scope:
  - "01-我的" ... "10-归档"
  exclude:
    - "---/workspaces"
    - ".claude/worktrees"
```

### manifest vault_rules 结构化段

**note-crafter** 新增 `depends_on`（6 个规则段）+ `produces_compliance`（3 个合规产出）。
**pjx-common-auditor** 新增 `validates`（10 个审计维度）+ `ignores`（2 个豁免目录）。

两文件 YAML 语法验证通过，信息与 vault-rules.yaml 实际条款一致（非凭空编造）。
