---
tags: [治理, skill]
description: note-crafter 从 Codex 迁移到 Hermes，集成 vault-index.yaml 快速路径解析。
type: note
create-date: 2026-05-01
---

# 26.0501-note-crafter-hermes-migration

## 变更

- **从 Codex 迁移** `~/.codex/skills/note-crafter/` → `~/.hermes/skills/note-taking/note-crafter/`
- **核心优化**：Step 2（定位归档目录）用 `99-设置/vault-index.yaml` search_routing 替代隐式「识别目录」+ 文件系统扫描
  - 关键词提取 → search_routing 查候选目录 → directories 查 MOC → 锁定路径
  - 减少 token 消耗（不再 `find`/`grep -r` 全量扫描）
- **对齐规范**：frontmatter 模板对齐 hxc-obsidian-vault（tags/description/type/create-date 四字段）
- **type 枚举**：与 vault-rules.yaml 一致，原 Codex 版用 `概念定义 | 工具参考 | 操作指南 | 规约 | MOC`

## 新增依赖

- `99-设置/vault-index.yaml`（同日创建）
- `99-设置/vault-rules.yaml`（含 cross_reference.vault_index）

## 保留项

- Codex 版 `note-crafter` 保留为 `/claude code` 命令使用（`/note-crafter`）
- 6 维度质量审查原样保留
- 命名规则、正文模板原样保留
