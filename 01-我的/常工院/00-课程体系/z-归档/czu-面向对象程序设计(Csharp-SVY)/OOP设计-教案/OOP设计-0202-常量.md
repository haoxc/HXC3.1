---
aliases: oop-C#@Fundamentals/Literals(Constants)
type: note
tags: [工具]
description: - 常量是`固定值`，程序执行期间不会改变, 也称为`字面量`。
create-date: 2026-04-29
---
## 概要
 - 常量是`固定值`，程序执行期间不会改变, 也称为`字面量`。
 - 常量可以是任何基本数据类型，比如整数常量、浮点常量、字符常量或者字符串常量，还有枚举常量。
 - 常量可以被当作常规的`变量`，只是它们的值在定义后不能被修改。

---
## 整数常量
- 整数常量可以是十进制、八进制或十六进制的常量。
- 前缀指定基数：`0x 或 0X `表示十六进制，0 表示八进制，没有前缀则表示十进制。
- 长整型的字面量,后面需要附带字符`l`或`L`
- 无符号的字面量,后面需要附带字符`u`或`U`
```csharp
85         /* 十进制 */
0213       /* 八进制 */
0x4b       /* 十六进制 */
30         /* int */
30u        /* 无符号 int */
30l        /* long */
30ul       /* 无符号 long */

```


---
## 浮点常量
- 一个浮点常量是由整数部分、小数点、小数部分和指数部分组成。
- 可以使用小数形式或者指数形式来表示浮点常量。

```csharp
float f1  = 236F;    //合法
double d1 = 236.714; //合法
double d2 = .1234;   //合法
double d3 = 7.33e-25; //合法 
```

---
## 字符常量
字符常量是括在单引号里，例如，'x'，且可存储在一个简单的字符类型变量中。在 C# 中有一些特定的字符，当它们的前面带有反斜杠时有特殊的意义，可用于表示换行符（`\n`)或制表符 tab（`\t`）

| 转义序列       | 含义             |
|------------|----------------|
| `\\ `        | \ 字符           |
|` \'  `       | ' 字符           |
| `\"`         | " 字符           |
| `\?`         | ? 字符           |
| `\a`         | Alert 或 bell   |
| `\b  `       | 退格键（Backspace） |
| `\f`         | 换页符（Form feed） |
| `\n`         | 换行符（Newline）   |
| `\r `        | 回车             |
| `\t`         | 水平制表符 tab      |
| `\v`         | 垂直制表符 tab      |
| `\ooo`       | 一到三位的八进制数      |
| `\xhh` . . . | 一个或多个数字的十六进制数  |

---
## 字符串
- 字符串常量是括在双引号` "" `里，或者是括在 `@` 里。
- 字符串常量包含的字符与字符常量相似，可以是：*普通字符、转义序列和通用字符*
- 使用字符串常量时，可以把一个很长的行拆成多个行，可以使用空格分隔各个部分。

```csharp
string a = "hello, world";                  // hello, world
string b = @"hello, world";               // hello, world
```
---

## 定义常量
常量是使用 **const** 关键字来定义的, 定义一个常量的语法如下：
```csharp
const <data_type> <constant_name> = value;
```

---
## 参考
- #pp-163-字面量
- [C# 常量 | 菜鸟教程](https://www.runoob.com/csharp/csharp-constants.html)
## 日志
- 创建时间
	- 2023年10月2日 14:53:42
- 变更时间
	