#### 课题4：批量素材处理类（进阶）
- **实践目标**：结合模块拆分，实现批量素材处理；
- **考查知识点**：模块设计、类的关联、列表管理实例、批量文件操作；
- **数媒场景**：批量修改素材尺寸、转换格式、添加水印；
- **实践内容**：
  1. 定义`MaterialBatch`类，属性为素材实例列表；
  2. 实现方法：`batch_resize()`（统一图片尺寸）、`batch_convert_format()`（转换格式）；
  3. 导入阶段1的`ImageInfo`类，结合实现批量处理；
- **交付物**：模块文件+处理前后的素材对比文件夹（截图）。