---
aliases:
  - abbr://看板/跟进事项(kanban@follow-up svy; kanban@gj-svy)
---

## 跟进事项

```dataview

TABLE 
  item.text as "内容",
  item.tags as "标签",
  item.section as "章节"
FROM "01-看板"
FLATTEN file.lists AS item
WHERE contains(item.tags, "#跟进_follow-up")
```
