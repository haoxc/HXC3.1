---
aliases:
tags:
description:
type:
ref-url:
create-date: 2026-04-21 22:05
---
```mermaid
flowchart TD
    %% 1. 准备阶段
    subgraph Preparation [1 准备阶段]
        A[项目规范 agents-specs] --> B[OpenHands 审计技能挂载]
        B --> C{真相源就绪?}
    end

    %% 2. 建模阶段
    subgraph Modeling [2 建模阶段]
        C -- 是 --> D[Codex 生成架构设计文档]
        D --> E[OpenHands 哨兵预审 Arch Spec]
        E -- 违反约束 --> D
        E -- 审核通过 --> F[锁定建模契约]
    end

    %% 3. 编程阶段
    subgraph Execution [3 编程阶段]
        F --> G[Codex 编写修改代码]
        G --> H[OpenHands 沙盒同步运行]
        H --> I{审计通过?}
        I -- 偏差 --> J[OpenHands 反馈审计报告]
        J --> G
        I -- 通过 --> K[提交至本地分支]
    end

    %% 4. 治理阶段
    subgraph Governance [4 治理阶段]
        K --> L[全量评估 DeepEval]
        L --> M{对齐度超过 90?}
        M -- 逻辑偏移 --> N[OpenHands 生成重构建议]
        N --> G
        M -- 达标 --> O[并入主干]
    end

    %% 样式美化
    style Preparation fill:#f9f2ff,stroke:#7d3fc0,stroke-width:2px
    style Modeling fill:#e6f4ff,stroke:#1677ff,stroke-width:2px
    style Execution fill:#f6ffed,stroke:#52c41a,stroke-width:2px
    style Governance fill:#fffbe6,stroke:#faad14,stroke-width:2px
```
